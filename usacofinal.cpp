#include <bits/stdc++.h>
using namespace std;
int main(){
    long long n, m;
    cin>>n>>m;
    string s;
    cin>>s;
    vector<long long> a(n,0);
    for (int i=0;i<n;i++){int y; cin>>y; a[i]=y;}
    a.push_back(a[0]); a.push_back(a[1]);
    s=s+s[0]+s[1];
    for (int j=0; j<m; j++) {
        for (int i=1;i<=n;i++) {
            
        }
    }
}