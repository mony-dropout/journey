#include <bits/stdc++.h>
using namespace std; 
int main()
{
   //store in adj[v] all edges originating from v as pair<to,weight>
   //int inf=1e7;
   /*
   void dijk(int s, vector<int> & dist, vector<int> & p)
   n=adj.size(); 
   fill(dist.begin(),dist.end(),inf)
   fill p with -1;
   vector<bool> vted(false);
   dist[s]=0; //set d[s]=0;
   //now start
   for (int i=0;i<n;i++) //this is the main loop, everything is under this
   { 
    int v=-1;
    for (int j=0;j<n;j++) //finding current min
    {
        if (!vted) && (v==-1 || d[j]<d[v]) 
        {j=v;}
    }
    //done, v is current min
    if (dist[v]) ==inf  break;
    else
    vted[v]=true;
    for (auto p:adj[v])
    {   
        int to=p.first;
        if !vted[to]
        int weight=p.second;
        if (d[v]+weight<d[to]) {p[to]=v; d[to]=d[v]+weight;}
        

    }



   }
   
   
   
   */
}